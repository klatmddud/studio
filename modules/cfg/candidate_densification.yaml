enabled: false
warmup_epochs: 10
lambda_dense: 0.05
min_severity: 1.0
record_match_threshold: 0.95
max_dense_targets_per_image: 5
base_extra_candidates: 2
severity_budget_scale: 1.0
max_extra_candidates_per_gt: 6
base_region_scale: 1.0
severity_region_scale: 0.1
max_region_scale: 1.5
require_unassigned_points: true
failure_types:
  - candidate_missing
  - loc_near_miss
  - cls_confusion
  - score_suppression
  - nms_suppression

models:
  fcos: {}
  fasterrcnn:
    enabled: false
  dino:
    enabled: false
