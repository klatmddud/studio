enabled: false
start_epoch: 2
warmup_epochs: 0
replay_ratio: 0.25
max_replays_per_batch: 0
replacement: true
replay_recency_window: 1
max_replays_per_record_per_epoch: 4
min_consecutive_count: 1
min_total_failed: 1

type_ratios:
  loc: 0.45
  background: 0.35
  missed: 0.10
  duplicate: 0.05
  cls: 0.025
  both: 0.025

replay_modes:
  loc: failure_aware
  cls: full_image
  both: full_image
  missed: full_image
  duplicate: full_image
  background: failure_aware

models:
  fcos: {}
  fasterrcnn: {}
  dino:
    enabled: false
