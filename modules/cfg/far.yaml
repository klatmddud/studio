enabled: true
lambda_far: 0.1
anchor_ema_mu: 0.9
persistence_gamma: 1.0
min_relapse_streak: 1
match_threshold: 0.95
warmup_epochs: 1
max_anchors_per_image: null
feature_keys: ["0", "1", "2", "p6", "p7"]
roi_output_size: 7
roi_sampling_ratio: 2

models:
  fcos: {}
  fasterrcnn:
    enabled: false
  dino:
    enabled: false
